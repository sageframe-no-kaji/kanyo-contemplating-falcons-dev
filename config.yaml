# Kanyo configuration
# Override any value with env var: KANYO_<KEY> (e.g., KANYO_VIDEO_SOURCE)

video_source: "https://www.youtube.com/watch?v=EXAMPLE"   # required
detection_confidence: 0.5      # 0.0–1.0
detection_interval: 60         # seconds between checks
output_dir: "output"           # results directory
model_path: "models/yolov8n.pt"
