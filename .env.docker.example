# Kanyo Environment Variables for Docker Deployment
# Copy this file to .env and fill in your values

# ──────────────────────────────────────────────────────────────
# Notification Secrets (REQUIRED)
# ──────────────────────────────────────────────────────────────
# Bot token - single token can post to multiple channels
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ──────────────────────────────────────────────────────────────
# Docker Volume Paths
# ──────────────────────────────────────────────────────────────
# Where to store clips and logs for each camera stream
KANYO_CAM1_ROOT=/opt/services/kanyo-harvard
KANYO_CAM2_ROOT=/opt/services/kanyo-nsw

# Where the kanyo source code is checked out (for volume mounts)
KANYO_CODE_ROOT=/opt/services/kanyo-code

# ──────────────────────────────────────────────────────────────
# Notes
# ──────────────────────────────────────────────────────────────
# - Channel and topic configs are in per-camera YAML files
# - This allows one .env with secrets to work with multiple streams
# - Each camera's config.yaml specifies its telegram_channel and ntfy_topic
