---
created: 2026-07-09
type: state-memory
project: kanyo
kamae: 6
status: living
---

# Kanyō — State Memory (Kamae 6)

This file is the build's living cross-session memory: hot, mutable, and non-canonical. It is read first by any fresh session to reconstitute build state quickly. The cold record — git history, per-ho Reflect sections in the devlog, and the build record — is always the source of truth; this file is a derived cache and yields to the cold record whenever they disagree.

---

**STATE-SUMMARY**
- **COMPLETED** — **Everything current, merged, and deployed as of 2026-07-18.** Sequence: v1.0.0 image migration (all four detectors off the src-mount onto pinned images) → **v1.0.1** deno hotfix (harvard went pure-live; YouTube now gates pure-live extraction behind a **PO Token** solved by a **deno** JS challenge — images had `node`/nsig but not deno, so harvard failed on BOTH old and 1.0.0 images while DVR streams nsw/umass were fine; fixed by adding pinned deno 2.9.3 to all three Dockerfiles, PR #49) → **PR #47 merged** (post-1.0 features: multi-bird count `BirdCountTracker` off by default, chick-season docs preset, FalconVisit row lifecycle, admin stream auto-discovery + Create-Stream form + compose-snippet gen, creature customization, YouTube constraints doc) → **v1.1.0 tagged and deployed**. **The whole fleet now runs `1.1.0-nvidia` (#47 + deno), `restarts=0`, all four resolving and detecting.** Admin dashboard rebuilt on #47 code and switched to the single `/opt/services:/data` parent mount with stream auto-discovery (reserved dirs excluded). Viewer also hit the same PO-token gap in its server-side live-URL resolver (`streams.py`) — fixed by adding deno to the viewer image (kanyo-viewer `95dd1ee`), and "Beta" brand badges removed (`8b1e1ef`); viewer live player recovered. Per-site tuning live: harvard (`presence_sustain_confidence 0.10`, `merge_window_seconds 120`, `min_significant_seconds 15`, `damping_arrivals_threshold 10`), fortwayne (`min_significant_seconds 60`, `damping_arrivals_threshold 6`). Issues closed: #5, #6, #8 (delivered by #47), #14 (auto). Filed **#50** to track the deno/PO-token work + doc follow-ups.
- **NEXT** — **2026-08-07: the chick-season field test failed and has been reverted; static-object false positives are the live problem.** The fortwayne "egg watch" never worked, because **Kanyō has no egg detection** — the chick-season preset only works by exploiting YOLO-visible *chicks* holding the nest occupied, and eggs are invisible to the detector. Worse, the Fort Wayne clutch had already failed: two abandoned, discoloured eggs, adults dispersed. What the preset actually produced was **~8 hours of false "visits" on 2026-08-07 alone** (2h54m + 4h59m, counting up to **4 birds on an empty scrape**), caused by YOLO reading the graffiti-tagged DNR sign on the nest-box camera angle as **`person(0)` at a confidence pinned flat at ~0.43** — class 0 was in that site's `animal_classes`. Real falcons there register as **`bird(14)` at 0.6–0.95**, on a *different* camera angle (the stream rotates between nest-box and ledge/skyline views); those detections were always genuine and are unaffected. **Fixed on the host:** fortwayne dropped class 0, reverted off the chick preset (`bird_count_enabled: false`, `merge_window_seconds 300`, `notification_cooldown_minutes 5`; `min_significant_seconds 60` + `damping_arrivals_threshold 6` retained), backup at `config.yaml.pre-postseason-20260807`. Thresholds deliberately NOT raised — real detections span 0.6–0.95 but false ones reached 0.64, so no threshold cleanly separates them; the class fix is surgical and the *flat-confidence* signature is the real discriminator. **Viewer:** egg-watch claim removed from the landing box, kanyo-viewer PR **#8** merged (`2fbddde`), rebuilt and redeployed, verified absent from the served bundle. **bigbear (Big Bear Bald Eagle Cam) deployed for the first time** — it had a config from 2026-04-21 but *no compose entry running* and had never started, while still being listed to visitors on the viewer as an empty card. Now up on `1.1.0-nvidia` via `docker compose --profile bigbear up -d bigbear-gpu`, with `creature_name: eagle` (logs read "🦅 EAGLE ARRIVED"). Its first ninety seconds reproduced the same failure in a new class: the **Jeffrey Pine trunk registers as `giraffe(23)` at up to 0.79** and confirmed a full arrival on an empty nest — so its `animal_classes` is narrowed to **`[14]` (bird only)**, to be widened only on this site's own evidence. Five detectors now running; zero false arrivals at fortwayne or bigbear since the fixes. Remaining: watch both for a clean 24–48h; field-validate bird-count against real footage before re-enabling anywhere (#3, #1, #2); live container orchestration (#7); direct-stream partnerships (#15).
- **ACTION ITEMS / BLOCKS** — No blocks. **Doc drift (tracked in #50):** `docs/deployment-kanyo.md` still says `1.0.0-nvidia` (prod is now `1.1.0-nvidia`) and assumes `sudo`/`ssh -t`/`mv`/`sed -i` — the real host mechanics are: deploy user `atmarcus` (uid 1000, `docker` group, **no passwordless sudo**), `/opt/services/kanyo-admin` **root-owned** (can overwrite existing files but not create new ones → use `cat`-overwrite, not `sed -i`/`mv`); backups went to `~/kanyo-v1.0.0-rollback/`. Fold the deno/PO-token finding into `docs/youtube-stream-constraints.md`. Policy: any image resolving YouTube live via yt-dlp must include deno (detector + viewer both do now). Phase 0 backups (`~/kanyo-v1.0.0-rollback/` + `*.pre-v1.0.0` in site dirs) — remove after ~a week clean. Carried forward: yolov8s upgrade as a later image once confidence-summary data accumulates (supersedes Agent Task 015); one-time host cleanup of stray `*.ffmpeg.log` predating 027; viewer eslint debt. Agent Tasks 013 + 017 landed via #47 and are now in production; 014, 016, 018, 019 open, priority unconfirmed. **New as of 2026-08-07:** (a) **the static-object filter is the outstanding architectural gap** — a detection whose confidence does not vary across N consecutive frames is furniture, not an animal; fortwayne sat at 0.427 for hours and bigbear at 0.79, and that signature would have killed both on frame twenty instead of hour five. This wants its own ho, not a config edit, and it is the general fix that per-site class pruning only patches. (b) **Per-site class lists must be derived from each site's own logs** — harvard/nsw/umass still carry class 0 deliberately (Harvard's at-lens birds misclassify as person/elephant) and were left alone, but none of them has been audited for a fortwayne-style static false positive; that audit is unclaimed work. (c) `docs/` still describes chick-season as a live capability at fortwayne and says nothing about eggs being undetectable — fold this in alongside the #50 doc drift. (d) fortwayne clips are at **319 GB** (ZFS 1.4T, 24%) and a meaningful share is footage of a sign; nsw 214 G, harvard 139 G, umass 63 G — no pressure yet, but retention policy is unaddressed. (e) New host backups: `config.yaml.pre-postseason-20260807` (fortwayne), `config.yaml.pre-deploy-20260807` (bigbear). (f) The telegram bot token sits in cleartext at `/opt/services/kanyo-admin/.env`; appropriately placed on-host, but rotate if that file's contents have travelled.
- **PROJECT LIFECYCLE** — `production`

_Seeded 2026-07-09; updated 2026-07-16 (tracking-rework close); updated 2026-07-18 at the close of the v1.0.0 → v1.0.1 → #47/v1.1.0 deploy (full fleet on 1.1.0-nvidia, everything merged and deployed); updated 2026-08-07 (chick-season reverted, person/giraffe static false positives fixed at fortwayne + bigbear, egg-watch claim pulled from the viewer, bigbear deployed for the first time)._
