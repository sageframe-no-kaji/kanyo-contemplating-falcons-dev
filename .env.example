# Kanyo Environment Variables
# Copy this file to .env and fill in your values

# ──────────────────────────────────────────────────────────────
# Notification Secrets (REQUIRED)
# ──────────────────────────────────────────────────────────────
# Bot token - single token can post to multiple channels
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ──────────────────────────────────────────────────────────────
# Per-Stream Configuration
# ──────────────────────────────────────────────────────────────
# Channel and topic are now in YAML config files:
# - test_config_nsw.yaml: telegram_channel: "@kanyo_nsw_falcon_cam"
# - test_config_harvard.yaml: telegram_channel: "@kanyo_harvard_falcon_cam"
#
# This allows one .env file with secrets to work with multiple streams

# ──────────────────────────────────────────────────────────────
# Optional: Backwards compatibility (deprecated)
# ──────────────────────────────────────────────────────────────
# If not set in YAML config, these env vars will be used:
# TELEGRAM_CHANNEL=@your_channel_name
# NTFY_ADMIN_TOPIC=your_admin_topic_name

# ──────────────────────────────────────────────────────────────
# Docker Volume Paths (for docker-compose)
# ──────────────────────────────────────────────────────────────
# Where to store clips and logs for each camera stream
KANYO_CAM1_ROOT=/opt/services/kanyo-harvard
KANYO_CAM2_ROOT=/opt/services/kanyo-nsw

# ──────────────────────────────────────────────────────────────
# Optional: Config Overrides
# ──────────────────────────────────────────────────────────────
# Recommended: Use config.yaml for these settings
# KANYO_VIDEO_SOURCE=https://www.youtube.com/watch?v=glczTFRRAK4
# KANYO_DETECTION_CONFIDENCE=0.7

# ──────────────────────────────────────────────────────────────
# Future Features (not currently implemented)
# ──────────────────────────────────────────────────────────────
# CLOUDFLARE_API_TOKEN=your_token_here  # For R2 storage
# YOUTUBE_API_KEY=your_key_here         # Alternative to yt-dlp
```

### Update .gitignore

**Make sure this is in `.gitignore`:**
```
# Environment variables
.env
*.env
!.env.example
