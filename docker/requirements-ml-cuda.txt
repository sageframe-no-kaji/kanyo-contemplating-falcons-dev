# ML detection backend - CUDA-enabled for NVIDIA GPUs
# Install AFTER requirements-base.txt to avoid dependency resolution chaos

# YouTube stream resolution (explicitly pinned for build-time stability)
yt-dlp>=2024.12.01

# PyTorch with CUDA 12.1 support
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1

# Computer vision
opencv-python-headless==4.8.1.78  # Video processing (headless for Docker - no GUI deps)

# Detection framework (install LAST, after PyTorch is locked)
ultralytics==8.3.0                 # YOLOv8 object detection
